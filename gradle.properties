org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true

# Build target. Pick any file name from versions/ with -Pmc=<target>,
# e.g. ./gradlew build -Pmc=26.2
default_target=26.1.2

# Toolchain — shared by every target
loader_version=0.19.5
loom_version=1.17-SNAPSHOT

# Mod Properties
mod_version=1.4.0
maven_group=net.fentbusgaming
archives_base_name=localweather
